# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'GithubRepositorySearcher' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GithubRepositorySearcher
  pod 'Moya/Combine'
  pod 'Resolver'
  pod 'SwiftLint'

  target 'GithubRepositorySearcherTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
    pod 'SwiftyMocky'
    pod 'CombineExpectations'
  end

  target 'GithubRepositorySearcherUITests' do
    # Pods for testing
  end

end
